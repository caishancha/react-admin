import type { ButtonVariantSize, ButtonVariants } from '../../ui';

export interface SCButtonProps {
  asChild?: boolean;
  className?: any;
  disabled?: boolean;
  loading?: boolean;
  size?: ButtonVariantSize;
  variant?: ButtonVariants;
}
